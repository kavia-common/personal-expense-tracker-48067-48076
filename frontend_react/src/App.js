import React from 'react';
import './App.css';
import AppRoutes from './routes/AppRoutes';

/**
 * Root App composition which renders the routes inside the shell Layout.
 */
function App() {
  return <AppRoutes />;
}

export default App;
