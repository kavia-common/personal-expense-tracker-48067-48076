# Frontend-only example env file
# You do not need any env file to run. If you want to be explicit, only these keys are relevant.

REACT_APP_FEATURE_FLAGS=charts
REACT_APP_EXPERIMENTS_ENABLED=false
# Optional if you want to override the default:
# REACT_APP_HEALTHCHECK_PATH=/health

# Leave backend URLs unset to remain in offline/local mode:
# REACT_APP_API_BASE=
# REACT_APP_BACKEND_URL=
